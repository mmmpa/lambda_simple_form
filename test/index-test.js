const assert = require('power-assert');
const handler = require('../lib/index').handler;

describe('handler', () => {
  describe('handler', () => {
    it('1', (done) => {
      assert.equal(handler({}), {})
    });

  });
});
